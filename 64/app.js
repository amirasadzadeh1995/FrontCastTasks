const price = 777;

// if (price > 59) {
//   message = `Has Discount!`;
// } else {
//   message = `Has Not Discount `;
// }
const message = price > 59 ? `Has Discount` : `Has Not Discount`;

console.log(message);
