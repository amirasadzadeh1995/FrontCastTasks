const cartItems = [`book1`, `book2`, `book3`]

let res = `you have ${cartItems.length} products in your cart.`

//console.log(res)

console.log(`first item is ${cartItems[0]}`)
console.log(`your last item is: ${cartItems[cartItems.length - 1]}`)